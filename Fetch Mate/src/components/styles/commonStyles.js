import { createTheme } from "@mui/material";

export const customParagraphStyle = {
  marginTop: "10px",
  color: "#333",
  fontSize: "1rem",
  textAlign: "center",
};

export const navBarTheme = createTheme({
  palette: {
    primary: {
      main: "#8C6A5D",
    },
  },
});
